# @sevana/orchestrator

Agent core and multi-agent loop. Hosts the orchestrator and the six specialised agents (concierge, shopper, logistics, merchandiser, retention, guardrail). Calls out to `@sevana/model-gateway` for reasoning/vision and to `@sevana/connectors` for catalogue/delivery/checkout/CRM.
